{
  "name": "clash-dashboard-local",
  "version": "1.0.0",
  "description": "A dashboard for Clash Royale clans, using a local Express server to protect the API key.",
  "main": "server.js",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "node-fetch": "^2.7.0"
  }
}
